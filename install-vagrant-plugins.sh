vagrant plugin install vagrant-cachier
vagrant plugin install vagrant-omnibus
# crashes vagrant - why? Is there more to installation? vagrant plugin install vagrant-aws

# Uninstalled during debugging of vagrant failure; reinstall and test to see
# if problem was isolated to vagrant-aws plugin.
# vagrant plugin install vagrant-rackspace
# vagrant plugin install vagrant-vbguest
